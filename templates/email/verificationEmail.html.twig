{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row justify-content-center  pt-5">
            <div class="row justify-content-between col-md-6">
                <div class="col-md-auto align-self-center ">
                    <p>{{ name }}, Beauty Routine greet you in the world of beauty and self-improvement!</p>
                    <p>To complete the registration, please, follow the link</p>
                    {% set url = [verifyUrl, verifyCode]|join('/') %}
                    <a href="{{ url('verification', {'verifyCode' : verifyCode}) }}">{{ url }}</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
