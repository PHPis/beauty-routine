<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="navbar-brand" >
        <img src="" width="30" height="30" class="d-inline-block align-top" alt="">
        Private profile</div>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('profile') }}">Homy <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link active" href="{{ path('user.sub.routine.show') }}">My programs</a>
            </li>
            <li class="nav-item">
                <a class="nav-link active" href="{{ path('user.sub.completed.routine') }}">My completed programs</a>
            </li>
            <li class="nav-item">
                <a class="nav-link active" href="{{ path('user.routine')  }}">Search of care programs</a>
            </li>
        </ul>
    </div>
    <div class="col-md-auto align-self-center ">
        <a href="{{ path('profile.edit') }}">Edit</a>
    </div>
    <div class="col-md-auto align-self-center ">
        <a href="{{ path('app.logout') }}">Log out</a>
    </div>
</nav>
